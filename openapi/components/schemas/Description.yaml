description: |
      The order description that appears:
      - In your merchant dashboard 
      - On the MultiSafepay payment page (for `type`:`redirect`)
      - On the customer’s bank statement (if supported by their bank)
example: Test order description
type: string
maxLength: 200