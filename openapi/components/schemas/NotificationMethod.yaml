description: |
  Sets the notification method.
  
  We recommend setting this to `POST` to reduce network load and increase security.

  See [Webhook](https://docs.multisafepay.com/developer/webhooks/).
type: string
enum: [POST, GET]
default: GET
example: POST