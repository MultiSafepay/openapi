description: |
  Sets the webhook [notification method](https://docs.multisafepay.com/developer/webhooks/).
  
  We recommend setting this to `POST` to reduce network load and increase security.
type: string
enum: [POST, GET]
default: GET
example: POST