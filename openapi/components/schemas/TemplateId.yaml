description: |
  Apply a template for styling [MultiSafepay payment pages](https://docs.multisafepay.com/payment-pages/styling/).

  If `payment_options.template` is also set, `payment_options.template` is used.
type: string