description: Information about the direct debit.
type: object
properties:
  direct_debit:
    description: The direct debit payment details.
    type: object
    properties:
      bic:
        description: The bank identification code (BIC).
        type: string
      holder_name:
        description: The name of the account holder.
        type: string
      iban:
        description: The international bank account number (IBAN).
        type: string
    required:
      - bic
      - holder_name
      - iban
  status:
    description: Sets whether the payment details are active. If inactive, leave empty.
    type: string
    enum: [active]
  type:
    description: The payment method.
    type: string
    enum: [direct_debit]
