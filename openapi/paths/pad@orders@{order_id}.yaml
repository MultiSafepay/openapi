get:
  description: Get information about a Pay After Delivery order.
  operationId: 'PAD::OrderDetails'
  parameters:
    - description: ''
      in: path
      name: order_id
      required: true
      schema:
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              success:
                type: boolean
            type: object
      description: ''
  summary: Get PAD order details
  tags:
    - Pay After Delivery (BETA)
