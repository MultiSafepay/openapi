post:
  description: Add payment details for a payment method to a customer's profile.
  operationId: addPaymentMethod
  parameters:
    - name: customer_id
      description: The unique identifier for the customer.
      in: path
      required: true
      schema:
        type: string
  requestBody:
    content:
      application/json:
        schema:
          $ref: '../components/requestBody/AddPaymentMethod.yaml'
    description: ''
    required: true
  responses:
    '200':
      description: Request successful
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                $ref: '../components/schemas/Success.yaml'
              data:
                description: Information about the payment method.
                type: object
                properties:
                  customer_id:
                    description: The unique identifier for the customer.
                    type: string
                    example: snyvhryxwuwbg
                  payment_method:
                    $ref: '../components/schemas/PaymentMethod.yaml'
    '422':
      description: Validation errors
      content:
        application/json:
          schema:
            $ref: ../components/errors/422Error.yaml
  summary: Add payment method
  tags:
    - Customers
get:
  description: List a customer's saved payment details.
  operationId: listPaymentDetails
  parameters:
    - name: customer_id
      description: The unique identifier for the customer.
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Request successful
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                $ref: '../components/schemas/Success.yaml'
              data:
                $ref: '../components/schemas/PaymentMethod.yaml'
    '422':
      description: Validation failure
      content:
        application/json:
          schema:
            $ref: ../components/errors/422Error.yaml
    '403':
      description: Invalid API key
      content:
        application/json:
          schema:
            $ref: ../components/errors/403Error.yaml
  summary: List payment methods
  tags:
    - Customers
