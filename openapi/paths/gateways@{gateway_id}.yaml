get:
  description: 'Get the name of the payment method for a particular gateway identifier.'
  operationId: GetGateway
  parameters:
    - description: 'The unique identifier of the gateway. **Not** case sensitive.' 
      in: path
      name: gateway_id
      required: true
      schema:
        allOf: 
          - $ref: '../components/parameters/gatewayIdentifiers.yaml'
        type: string
  responses:
    '200':
      content:
        application/json:
          schema:
            properties:
              success:
                type: boolean
              data:
                type: object
                properties: 
                  id:
                    type: string
                    description: 'The unique identifier of the gateway.' 
                    example: "MISTERCASH" 
                  description: 
                    type: string
                    description: 'The name of the payment method.'  
                    example: "Bancontact"
            type: object
      description: ''
    '404':
      content:
        application/json:
          schema:
            $ref: '../components/schemas/GenericErrorResponse.yaml'
      description: Error 
  summary: Get gateway
  tags:
    - Payments

